import React from "react";

export const metadata = {
  title: "Checkout | UW Outers Club",
};

export default function CheckoutLayout({ children }: { children: React.ReactNode }) {
  return children as React.ReactElement;
}
